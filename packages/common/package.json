{
  "name": "@carto/common",
  "version": "1.0.0",
  "description": "contains common packages for all other packages",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "dependencies": {
    "find-up": "^4.1.0"
  },
  "scripts": {
    "build": "rimraf [dist tsconfig.build.tsbuildinfo] && tsc -p tsconfig.build.json"
  }
}
